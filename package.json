{
  "name": "shibunyan",
  "version": "0.3.0",
  "description": "mikunyan in TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepack": "npm run build",
    "build": "rm -r dist && tsc && make && rollup -c"
  },
  "directories": {
    "lib": "dist"
  },
  "dependencies": {
    "@rinsuki/lz4-ts": "^1.0.1",
    "binarin": "^0.2.1"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^20.0.0",
    "@rollup/plugin-node-resolve": "^13.0.4",
    "@types/node": "^14.0.13",
    "rollup": "^2.56.3",
    "rollup-plugin-terser": "^7.0.2",
    "typescript": "^4.3.5"
  },
  "author": "",
  "license": "MIT",
  "bin": {
    "shibunyan-image": "./bin/image.js"
  }
}
